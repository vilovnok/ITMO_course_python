[project]
name = "richard-decorators"
version = "0.1.3"
description = "Небольшой набор переиспользуемых декораторов для проверки типов, кэширования и повторных попыток."
readme = "README.md"
authors = [{ name = "Гурциев Ричард" }]
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.3,<0.12"]
build-backend = "uv_build"